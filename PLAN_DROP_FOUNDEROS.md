# FounderOS plan — DROPPED

Owner switched to independent **Aether AI**.

- Active project: `aether-ai/` (this folder) + data at `E:/AetherAI/data`
- FounderAI continuous train: stop via `E:/FounderOS/runtime/founder-ai/training/STOP`
- Full FounderOS monorepo mass-delete is **not** auto-run (irreversible). Owner may delete after backup.
